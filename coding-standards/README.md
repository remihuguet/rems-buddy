# coding-standards

## Skill

- **naming-and-comments** — docstrings off by default, comments stripped to the vital minimum and never a changelog, domain vocabulary in names

Deliberately narrow: it carries only the conventions that differ from what Claude does unprompted. Generic advice like "avoid variables named `data`" was removed because it changed no behavior.
